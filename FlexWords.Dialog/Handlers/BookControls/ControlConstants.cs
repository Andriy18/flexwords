using System.Windows;

namespace FlexWords.Dialog.Handlers
{
    public static class ControlConstants
    {
        // TODO: remove before release
        public static readonly bool value = false;

        public static readonly Size MeasureSize = new(double.PositiveInfinity, double.PositiveInfinity);
        public static readonly Thickness BaseMargin = new();
    }
}
